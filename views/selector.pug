extends layout

block content
  h1= title
  - let image_names = ["GOT1.jpg", "GOT2.jpg", "HPL1.jpg", "SG1.webp", "SG2.webp"]
  - for (var i=0; i<3;i++) {
      - let idx = Math.floor(Math.random()*5);
      img(src=`/images/${image_names[idx]}`, alt="", height="300", width="300")
  - }